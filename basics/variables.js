// rule 1. you cannot define a variable more than once

let petName = 'Blaze';
// let petName = 'max'; // ex. for rule 1
petName = 'Max'; // you can only reassign a variable like this

console.log(petName);

// rule 2. there are rules related to variables names
//  let 3 = 2; ex. for rule 2 - cannot use a number or symbols first for variable
let result = 3+ 4;

// rule 3. variable names cannot be reserved keywords
// let let = 12 ex. for rule 3
